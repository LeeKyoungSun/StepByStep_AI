# --- API 서버 ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
python-dotenv==1.0.1

# --- HTTP 클라이언트/테스트 ---
httpx==0.27.2
pytest==8.2.1

# --- RAG/유틸 (필요시) ---
# OpenAI SDK: langchain 등을 쓸 수 있으니 하위 호환보다 넉넉히 잡음
openai>=1.99.9,<2.0.0

# 검색/임베딩 유틸
faiss-cpu==1.8.0
rank-bm25==0.2.2
tiktoken==0.7.0
orjson==3.10.0
tqdm==4.66.0

# numpy는 여러 라이브러리의 하한선 안정 버전으로
numpy>=1.26.2,<2